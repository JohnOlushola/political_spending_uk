def aggregate_items(original_id, description, price, custom_label):
  aggregate_array = []
  for item in data_items:
    if original_id==original_id and custom_label==custom_label:
      aggregate_array.append(id, sum, custom_label)

  return aggregate_array


# class BreadSticks(self, original_id, description, price, custom_label):
#   def __init__():
#     pass